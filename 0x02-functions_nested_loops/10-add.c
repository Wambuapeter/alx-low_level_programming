#include <stdio.h>
/**
 *add- adds two numbers and returns result
 *@a: first number
 *@b: second number
 *Return: result of two numbers
 */
int add(int a, int b)
{
	int a, b, result;

	result = a + b;
	Return(result);
}
