#include <stdio.h>
/**
 *add- adds two numbers and returns result
 *@a: first number
 *@b: second number
 *Return: result of two numbers
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
